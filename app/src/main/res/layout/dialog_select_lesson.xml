<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="@drawable/radius_layout">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="레슨 추가하기"
            android:textSize="26sp"
            android:textStyle="bold"
            android:textColor="@color/black"
            android:padding="10dp"
            android:gravity="center"
            android:layout_margin="5dp"
                />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_write_lesson_note"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="레슨 리뷰 작성"
        android:textSize="18sp"
        android:textColor="@color/black"
        android:layout_margin="5dp"
        android:background="@drawable/radius_view"
        android:gravity="center"
        android:layout_marginBottom="50dp"
        android:elevation="10dp"
        app:iconTint="@null"
        app:iconTintMode="add"
        app:icon="@drawable/ic_write_lesson_note"
        app:iconGravity="textStart"
        app:backgroundTint="@color/white"
        />
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_add_schedule"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="레슨 예약하기"
        android:textSize="18sp"
        android:textColor="@color/black"
        android:layout_marginStart="5dp"
        android:layout_marginEnd="5dp"
        android:layout_marginTop="5dp"
        android:layout_marginBottom="50dp"

        android:background="@drawable/radius_view"
        android:gravity="center"
        android:elevation="10dp"

        app:iconTint="@null"
        app:iconTintMode="add"
        app:icon="@drawable/ic_reserve_lesson"
        app:iconGravity="textStart"
        app:backgroundTint="@color/white"
        />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btn_back"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="뒤로가기"
        android:textColor="@color/white"
        android:textSize="18sp"
        android:background="@drawable/btn_cancel"
        android:layout_margin="5dp"
        />
</LinearLayout>